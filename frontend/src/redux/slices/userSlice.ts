import { createSlice } from "@reduxjs/toolkit";

export default createSlice({
    name: 'user',
    initialState: {
        
    },
    reducers: {
        setProfile: (state, action) => {
            
        },
    }
});