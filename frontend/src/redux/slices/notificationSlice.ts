import { createSlice } from "@reduxjs/toolkit";

export default createSlice({
    name: 'notification',
    initialState: {
        
    },
    reducers: {
        setNotification: (state, action) => {
            
        },
    }
});